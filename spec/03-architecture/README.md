---
navigation:
  order: 30
---

# 3. 構成

同期はクライアント、同期 API、ストア、通知の 4 つの要素で成り立つ（[構成要素](components.md)）。端末からサーバーへ、サーバーから他の端末へ変更が届く順序を [同期の流れ](sync-flow.md) に示す。順序の根拠は [版番号](versioning.md) であり、同じ版への更新が重なったときの扱いを [競合の解決](conflicts.md) に定める。
